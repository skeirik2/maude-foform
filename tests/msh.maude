load ../src/msh.maude

set print color on .
select MSH .

reduce

claim [ strategy < print("hello") >
        output < .Output >
      reduces-to
        strategy < .Program >
        output < print("hello") >
      ]
.
